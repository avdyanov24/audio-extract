{
  "name": "audio-extract",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local YouTube audio extraction tool. Runs on localhost only.",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c gray,yellow \"npm --prefix server run dev\" \"npm --prefix web run dev\"",
    "build": "npm --prefix web run build",
    "start": "npm --prefix server run start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20"
  }
}
